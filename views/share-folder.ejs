<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Share A Folder</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black/80 text-white">
  <main class="mx-auto w-full lg:w-3/4 p-4">
    <%- include('./partials/navbar') %>
    <div class="md:w-3/5 mx-auto mt-10">
      <form action="/folders/<%= folderId %>/share-folder" method="post" class="w-full mx-auto space-y-4 bg-gray-800 p-5 rounded-md shadow-md">
          <h2 class="text-2xl font-semibold pb-3 border-b-2 mb-8">Share Folder</h2>
          <div class="flex flex-col space-y-2">
            <label for="duration">Duration in Days</label>
            <input class="p-2 bg-transparent border rounded-md focus:outline-none" type="number" min="1" value="<%= (data && data.duration) ? data.duration : '' %>" name="duration" id="duration" placeholder="Enter a duration" required>
          </div>
          <button class="w-full p-3 bg-blue-600 rounded-md" type="submit">Share Folder</button>
      </form>
      <%- include('./partials/errors') %>
  </div>
  </main>
</body>
</html>